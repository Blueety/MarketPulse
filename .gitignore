# Dependencies
node_modules/
.venv/
venv/
__pycache__/
*.pyc

# Build output
dist/
build/
*.egg-info/

# Environment
.env
.env.local
.env.*.local

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Logs
*.log
!data/alerts.log
npm-debug.log*

# Coverage
coverage/
htmlcov/
.pytest_cache/

# Runtime-generated data (运行时自动创建，不入 git)
reports/
# 三十一期：SQLite 库不入 git（Railway 经 data/backup/ 恢复链取数）
data/marketpulse.db
data/marketpulse.db-wal
data/marketpulse.db-shm
# data/backup/ 必须保持入库（Web 启动恢复链的唯一数据源，勿加进忽略清单）

# Agent work (保留 tasks/，忽略临时文件)
# 用户自定义配置（不入库；用户可改阈值，系统回退内置默认）
config.json
tasks/*/tmp/

# Agent 工作记忆（2026-09-16）：.workbuddy/ 存跨会话的项目记忆与技能产物
# 不入库；⚠️ 它是工作记忆**本体**（不是缓存），本地目录不要删，否则丢历史日志
.workbuddy/
