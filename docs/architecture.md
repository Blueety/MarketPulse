# 架构说明（MarketPulse — 波动率监控系统）

> 描述项目的模块边界、数据流和关键设计决策。
> Agent 修改架构相关代码前必须先读这份文档。

## 概览

Python 项目。每个交易日有多个运行点：

1. **收盘日报**：`daily_report.py`（美东收盘后运行），从 Yahoo Finance 拉取美股（GSPC/IXIC/VIX/VXN/MOVE）+ A 股（SH/SZ/CYB）+ 另类资产（GLD 黄金 ETF / BTC-USD 比特币）共 10 个标的，生成 Markdown 日报（含分市场近 30 日趋势图），追加历史数据并缓存当日值，生成 `context/YYYY-MM-DD.json` 上下文（供 Hermes 常规解读/异动归因），交由 Hermes 读取并推送到 QQ 机器人。另类资产仅纳入日报「💰 另类资产」板块与趋势图，不参与告警、不进入波动率/大盘面板。
2. **盘中快照**：`snapshot_report.py`（A 股午盘 11:30 / A 股收盘 15:00 / 美股开盘 21:30 / 美股午盘 00:00，北京时间），按 `--market {a-share,us,alt}` + `--time {open,midday,close,noon}` 取对应市场子集（alt = GLD/BTC 另类资产单板块），渲染单板块简报存盘 `reports/snapshots/YYYY-MM-DD-{market}-{time}.md`，检查告警阈值（只读缓存基准），不推送。
告警：两入口各自检查当日变化率是否超过阈值（默认 VIX/VXN ±20%、MOVE ±15%、GSPC/IXIC ±4/±4.5、SH/SZ ±4%、CYB ±5%，env 可覆盖），
触发则生成 `alerts/YYYY-MM-DD-{type}.md`（type = close / a-share-midday / a-share-close / us-open / us-noon），由 Hermes 检测并独立推送一条警报消息；
同一指数当日只告警一次（午盘触发则收盘跳过），去重状态记在 `data/alerts.log`。

**脚本自身不包含推送逻辑。** AI 常规解读与异动归因由 Hermes 读取 `context/` 后生成（交付配置项，非仓库文件）。

## 模块划分

| 模块 | 路径 | 职责 |
|---|---|---|
| 编排入口 | `daily_report.py` | 收盘流程编排：取数 → 读缓存/历史 → 算涨跌幅 → 渲染报告 + 趋势图 → 写报告 → 追加历史 → 写缓存 → 生成 context |
| 编排入口 | `snapshot_report.py` | 盘中快照流程编排：按 `--market`/`--time` 取市场子集 → 分类 → 渲染单板块快照 → 落盘 `reports/snapshots/YYYY-MM-DD-{market}-{time}.md`（只读缓存作告警基准，不算涨跌幅、不写 history、不推送）；4 个 Hermes cron 各传一组参数 |
| 告警 | `src/alerter.py` | 告警文件渲染（附录块格式）、alerts.log 去重状态读写、`collect_breaches` 纯计算导出、`run_alert_checks` 编排（逐指数容错） |
| 数据获取 | `src/fetcher.py` | Yahoo 取数（含重试/退避/源间节流），SYMBOLS 注册表；八期新增 fetch_sector_heat 概念板块热度 Top5（AkShare/新浪源，线程限时 10s，失败返回 []）；十八期新增 SECTOR_MAPPING + aggregate_sectors，fetch_sector_heat 内部将全量概念板块聚合为 10 大类 +「其他」（成交额加权，零成交额简单平均，未匹配归其他，行契约同构） |
| 纯逻辑 + 持久化 | src/analyzer.py | 状态分类、涨跌幅、格式化、路径常量（含 CONTEXT_DIR）、last_values 缓存、history 读写（90 天滚动、原子写、损坏容错）、build_search_keywords、compute_correlation（指数对 Pearson 相关性，纯 Python 零依赖，输入为收益率） |
| 报告渲染 | src/reporter.py | Markdown 日报 / 午盘快照渲染、趋势图（matplotlib 懒加载 + 15s 线程限时）、分市场趋势图（us 2×1 / cn 3×1 双图，5s 限时）、落盘、相关性分析章节与 context correlation 键（generate_context，原子写） |
| 配置加载 | `src/config.py` | 阈值配置：config.json + 环境变量覆盖 + 内置默认三级（DEFAULTS/ENV_MAP/env_float/load_config），白名单校验，零依赖 |
| 图片渲染 | `src/image_renderer.py` | 日报图片化：md 解析（日期 / 10 指数卡片 / 趋势图引用 / AI 解读章节）/ 告警附录块解析 / Jinja2 模板渲染 / Playwright 截图转 PNG（宽 600 自适应高、全链路容错，失败返回 None 不影响主流程；注意：15s 超时 / ≤800KB 尺寸守卫 / zoom 重试已在 a536888 删除，当前未实现） |
| 报告输出 | `reports/YYYY-MM-DD.md`、`reports/snapshots/YYYY-MM-DD-{market}-{time}.md`、`reports/charts/YYYY-MM-DD{-trend,-us-trend,-cn-trend}.png`、`reports/images/YYYY-MM-DD.png` | 生成的 Markdown / 图片（日报趋势图统一用美东日期；图片为图片化推送产物，保留 .md 同时生成 .png） |
| 告警输出 | `alerts/YYYY-MM-DD-{type}.md`（type = close / a-share-midday / a-share-close / us-open / us-noon）、`data/alerts.log` | 告警文件 / 当日去重标记（运行时生成，gitignore 排除） |
| 数据持久化 | `data/last_values.json`（涨跌幅基准）、`data/history.json`（近 90 日历史） | 运行时生成，gitignore 排除 |
| Web 看板 | `web/app.py`（FastAPI 应用 + 5 端点）、`web/templates/index.html`、`web/static/app.js`、`web/static/style.css`、`web/__init__.py` | 只读看板（**bento 栅格仪表盘**，2026-09-11 重构）：`.dash > .row-kpi/.row-main/.row-3/.row-news` 四视觉行（1920 档 5/2/3/4 列、1280 档 KPI 3 列与 row-main 堆叠、375 档单列 + 抽屉）；解析 `data/history.json` / `context/*.json` / `alerts/*.md` 渲染 4 张 KPI 卡（含 sparkline）/ 趋势**四图合一 + 4 类别 tab**（单 `canvas#chart-main`，7D/30D/90D/1Y）/ 自选列表（CSS 迷你条）/ 市场概览 6 小卡 / A 股板块 Top5 / 美股行业板块横幅条 / 告警记录，另 3 个静态占位卡（`data-placeholder="1"`）；提供 `/api/history`（`days` 上限 **365**）/ `/api/latest`（`sector_heat` + **`us_sector_heat`**）/ `/api/alerts` / `/api/watchlist`（实时取数，零写盘）/ **`/api/macro`**（env `MACRO_STOCKS` > `config.json` `macro.stocks` > 内置默认，TTL 90s）五个 JSON API；零侵入 `daily_report.py` / `snapshot_report.py` / `src/*`，进程绝不写任何数据文件 |
| SQLite 存储 | `src/storage.py`（三十一期新建） | history 长表 (date, symbol, value, change) 的建表/upsert（preserve|overwrite 双模式）/范围查询/长宽互转/按月备份导出/空库恢复；WAL；标准库 sqlite3；`DB_PATH` 调用时属性查找（测试单点 patch，对"补丁打使用方"纪律的有意例外） |
| 回测 | `scripts/backtest.py` | 独立回测脚本：复用生产 `check_breach` 语义回放历史触发事件，统计告警次数 / 年化频率 / WARN-ALERT 分布 / 1·3·5·10 日平均后效 / 胜率 / 有效触发率；只读 `data/history.json`，仅写 `reports/backtest_report.md`，不联网、零副作用 |
| 自动提交推送 | `src/git_ops.py` | cron 入口（daily_report / snapshot_report / opening_analyzer）main() 末尾调用 `auto_commit_push(date, report_type)`：env `AUTO_PUSH` ≠ "0" 启用（`"0"` 关闭）；无改动（`git status --porcelain` 空）跳过、幂等；`git add -A` + `git commit -m "auto: {date} {type}"` + `git push origin master`（经 Clash 代理 `http://127.0.0.1:7890`，仅注入 push 子进程 env 副本）；失败仅记日志、退出码恒 0；纯 stdlib 零新依赖（二十六期） |

## 数据流

```text
Yahoo Finance (^VIX, ^VXN) ──┐
                             ├──> daily_report.py ──> reports/YYYY-MM-DD.md ──> Hermes ──> QQ 推送
                                        ├──> reports/images/YYYY-MM-DD.png（图片化推送产物；Hermes 追加 AI 解读后经 scripts/render_report_image.py 重渲染含解读图）
                                        ├──> reports/charts/YYYY-MM-DD-trend.png（报告中引用）
                                        ├──> data/history.json（按 date 追加/覆盖，90 天滚动）
                                        ├──> data/last_values.json（今日值，次日作涨跌幅基准）
                                        └──> context/YYYY-MM-DD.json（indices + history_30d + breach + search_keywords）
                                              └──> Hermes 常规解读 /（异动日）tavily 搜索归因 ──> 追加日报 ──> QQ 推送

两入口（收盘/午盘）另检查变化率告警（基准 data/last_values.json 旧缓存，只读）：

    daily/snapshot ──> src/alerter.run_alert_checks ──> alerts/YYYY-MM-DD-{type}.md ──> Hermes ──> QQ 独立推送
                                        └──> data/alerts.log（当日已告警标记，午盘触发则收盘跳过）

    snapshot_report.py ──> reports/snapshots/YYYY-MM-DD-{market}-{time}.md（仅存盘，不推送）
```
cron 自动提交：daily_report / snapshot_report / opening_analyzer 在 `main()` 末尾调用 `src/git_ops.auto_commit_push(date, report_type)`，将本次 `data/`（history/last_values）、`context/`、`alerts/` 等变更 `git add -A` + commit（`auto: {date} {type}`）+ push 到 `origin/master`（经 Clash 代理 `http://127.0.0.1:7890`），确保 Railway 部署与最新数据同步；无改动跳过、失败仅记日志（详见关键决策表「cron 自动提交推送」）。

## 关键决策

| 决策 | 选择 | 原因 | 日期 |
|---|---|---|---|
| 模块拆分 | `daily_report.py` 300 行拆为 `src/` 三模块 + 约 50 行编排入口 | 二期新增快照与趋势图后单文件不可维护；`snapshot_report.py` 复用 fetcher | 2026-09-01 |
| 数据源 | 三指数均用 Yahoo：^VIX / ^VXN / ^MOVE | FRED 公开 API 无 MOVE 序列；^MOVE 标名有误但数值真实（与 Investing.com 一致） | 2026-08-29 |
| 时区 | 内部 UTC，报告显示美东日期 | 避免时区混淆导致日期错 | 2026-08-29 |
| 容错 | 单数据源失败继续运行，不整体崩溃；任一指数取数失败在 history 中存 null | 一个源挂了不能吞掉整个日报；趋势图按 NaN 断点处理 | 2026-08-29 / 2026-09-01 |
| 历史存储 | `data/history.json` 列表按 date 键追加/覆盖，仅保留最近 90 条，临时文件 + `os.replace` 原子写，损坏按空历史处理 | 趋势图数据来源；避免无限膨胀与半截写入 | 2026-09-01 |
| 趋势图 | matplotlib（Agg 后端 + 懒加载）；Windows 无 SIGALRM，用 daemon 线程 `join(3)` 限时，超时跳过绘图、报告不产生死链 | 无头环境渲染；冷启动/渲染超时不中断整体流程 | 2026-09-01 |
| 图表语言 | 趋势图标签一律英文（"VIX (30D)" / "VXN" / "MOVE" / "Date" / "Value"） | 中文字体跨平台渲染不一致（PRD 强制约束） | 2026-09-01 |
| 告警基准 | 统一用 `data/last_values.json` 旧缓存；快照只读不写 history；收盘告警检查置于 save_last_values 之前 | 避免误用当日新缓存；多时点不并发写（已确认决策 1/决策 G） | 2026-09-01 |
| 告警去重 | 当日已触发状态记 `data/alerts.log`（行式 "YYYY-MM-DD SYMBOL"，原子重写仅当日行）；午盘触发则收盘跳过同一指数 | 同一指数当日只告警一次（已确认决策 2/设计 C） | 2026-09-01 |
| 告警阈值 | 默认 VIX/VXN ±20%、MOVE ±15%；env `ALERT_THRESHOLD_<SYM>` 覆盖；触发条件为变化率**严格大于**阈值 | 默认值已确认；env 非法/非正回退默认（已确认决策 3/设计 A） | 2026-09-01 |
| 告警级别 | 触发即 WARN；当前值处于恐慌区间（classify 判定）升级为 ALERT | PRD 要求 WARN/ALERT，复用已确认 classify 区间，零新增配置（设计 B） | 2026-09-01 |
| 告警文件 | `alerts/YYYY-MM-DD-{type}.md`（type = noon / close），多指数各占一个附录块（frontmatter + 标题 + 字段） | PRD 固定文件名 {type}；多指数同日触发不冲突（设计 D） | 2026-09-01 |
| 告警容错 | run_alert_checks 内逐指数 try/except，调用方再包一层 try/except，仅记日志 | 告警逻辑失败不影响日报生成，退出码恒 0（决策 H） | 2026-09-01 |
| context 计算单一来源 | alerter 新增纯计算导出 `collect_breaches(values, last_values)`（遍历 check_breach，不写文件/不改 alerts.log，幂等）；`run_alert_checks` 重构为复用它（去重/写文件/标记逻辑原样） | breach 判断单一事实来源，context 生成不产生副作用；既有 23 条 alerter 测试行为等价锁定（决策 A） | 2026-09-01 |
| context 契约 | context/YYYY-MM-DD.json 八键：date / indices（value/change_pct/status）/ history_30d（dates + gspc/ixic/sh/sz/vix/vxn/move/cyb/gld/btc 等长数组，含当日）/ breach（triggered + indices 字段 name/current/previous/change_pct/threshold/level，level 沿用 WARN/ALERT）/ sector_heat（板块热度 Top5：name/change/turnover/top_stock）/ us_sector_heat / search_keywords / correlation（显著相关对：a/b/pair/r/n，|r|>0.5 才写入） | PRD 字段表定稿，Hermes Prompt 按此编写；_breach_item 纯映射单测锁定（决策 B）；八期新增 sector_heat 键（决策 H）；十一期新增 us_sector_heat 键；十二期新增 correlation 键（仅 |r|>0.5 显著对） |
| 搜索关键词 | `build_search_keywords(date, breaches)` 方向感知：异动指数变化率 >=0 用 "surge"、<0 用 "drop"，加 market volatility / economic data 定向词，异动日 3-5 个；常规日 1 个 "market summary {date}" | 方向感知直接服务归因搜索相关性（VIX 下跌时搜 "VIX drop" 才能命中下跌原因）（决策 C） | 2026-09-01 |
| context 生成时机 | `generate_context` 在 `append_history` 之后、`save_last_values` 之后调用（history_30d 含当日）；临时文件 + `os.replace` 原子写；仅收盘入口生成，snapshot 不动 | 含当日的 30 日趋势对 Hermes 参考更完整；原子写避免读到半截 JSON（决策 D/F） | 2026-09-01 |
| context 容错 | `generate_context` 不吞异常，`daily_report.py` 调用方 try/except 兜底仅记日志，退出码恒 0 | 失败路径可见可测；context 失败不影响日报主流程（决策 E） | 2026-09-01 |
| AI 解读/归因 | Hermes 侧 Prompt + tavily 搜索为交付配置项（非仓库文件）；Python 侧不引入 LLM/搜索 SDK | PRD 明确约束（决策 G） | 2026-09-01 |
| 阈值配置化 | 五期将硬编码阈值外置到 config.json + env 覆盖；优先级链 env > config.json > 内置默认，config 缺失/非法回退默认不崩溃 | 改配置不改代码；为加股票/新指标铺配置基建（设计 A-G 已确认） | 2026-09-01 |
| 盘中快照（七期） | 快照扩展为 4 个市场时点：A 股午盘 11:30 / A 股收盘 15:00 / 美股开盘 21:30 / 美股午盘 00:00（北京时间）；`--market a-share` 取 SH/SZ/CYB、`--market us` 取 GSPC/IXIC（不含波动率）；创业板 `399006.SZ` 入 SYMBOLS（8 键，阈值 ±5%）；快照存 `reports/snapshots/YYYY-MM-DD-{market}-{time}.md`，告警文件复合名 `alerts/YYYY-MM-DD-{market}-{time}.md` 防与日报碰撞；A 股快照按北京时间归档（设计 A-G） | 盘中快照原仅美东 12:30 三板块；现按市场/时段分档，波动率仅保留在日报，快照单板块；旧 `YYYY-MM-DD-noon.md` 命名退役 | 2026-08-30 |
| A 股板块热度（八期） | 日报新增「🔥 A 股热点板块 Top 5」：AkShare `stock_sector_spot(indicator="概念")` 取概念板块，按涨跌幅降序取 Top5（不设阈值）；`fetch_sector_heat` 线程限时 10s（新浪源无 timeout），超时/异常/缺必需列返回 [] 不中断日报；板块名注入 `search_keywords`（方向感知 surge/drop，不触发独立告警）；context 新增 `sector_heat` 键 | 丰富市场情绪感知；Top5 方案避免阈值主观设定；新浪接口挂起由线程限时兜底；板块热度非核心，失败降级为「数据暂缺」 | 2026-08-30 |
| 分市场趋势图（九期） | 日报新增美股 2×1（GSPC/IXIC）与 A 股 3×1（SH/SZ/CYB）趋势图：`render_market_trend_chart(history, date, market)`（注册表式，market∈{us,cn}，复用 render_trend_chart 绘图范式，独立 `MARKET_CHART_TIMEOUT=5` 限时，三图串行渲染）；`render_report` 加 `us_trend_chart`/`cn_trend_chart` 默认参数 + 两个章节（us 图在美股大盘后、cn 图在 A 股大盘后）；占位文案英文 "Insufficient Data"；市场键 us/cn 与快照 `MARKETS` 键 a-share/us 不同；`daily_report.py` 单次 `load_history` 复用 | 波动率图外补足分市场走势感知；图表文本英文是既有硬约束；整体行数<2 返回 None 省略章节（与 render_trend_chart 一致），部分序列缺数据子图占位不中断 |
| Web 看板（十一期） | 新增 `web/` 包：FastAPI 单页看板 + 3 个只读 JSON API（`/api/history` / `/api/latest` / `/api/alerts`）。路径常量复用 analyzer 单一事实来源，但在 `web/app.py` 重新绑定为模块级名字（解析函数一律引用本模块常量），测试 monkeypatch 落点严格打在使用方模块 `web.app`（打在定义方 analyzer 不生效）；板块热度数据源为最新 `context/*.json` 的 `sector_heat`（history.json 无板块字段，PRD 字面「从 history.json」不实，按「单独 JSON = context」落地）；`alerts/` 空目录 / 坏文件容错返回 `[]` 不 500；Chart.js 走 CDN，加载失败图区降级「图表加载失败」文案；新增运行依赖 fastapi/uvicorn/jinja2/httpx（决策 A，PRD 技术栈明确指定） | 浏览器展示最近 7 日市场数据，零侵入日报/快照主流程；只读语义（不读 `last_values.json`、不写任何生成物）保持与日报同一事实来源；量级悬殊（BTC 万级 vs VIX 十几）按市场分 4 图各独立 y 轴 | 2026-08-30 |
| Web 看板板块数据兜底（context 空壳回退，2026-09-03） | `_load_latest_context` 升级为「最近有效 context」：按文件名倒序遍历，返回第一个 `sector_heat` 为 dict 且 `gainers` 非空的 context（= 最近一次板块取数成功的交易日）；全部无板块数据 → 返回倒序第一个可解析 context；目录缺失/全坏 → `None`；新增 `_read_context_file` 逐文件容错（坏 JSON/非 dict/IO 错误跳过继续向前回退）。`_load_sector_heat` 与 `api_latest` 零改动，板块列与状态列自动同源回退；前端、`api_latest` 其余字段、数据生产端 `generate_context` 一律不改（方案 A） | 失败空壳 context（`sector_heat` 空、indices 全 null，由全源取数失败运行写出）按文件名严格取最末会遮蔽前一交易日真实板块数据，前端显示「数据暂缺」；用 `gainers` 非空作「该次板块取数成功」的可靠标记 | 2026-09-03 |
| 测试隔离 | tests/conftest.py 顶层强制 CONFIG_PATH 指向不存在文件，collection 前生效 | 用户定制 config.json 后跑 pytest 不破坏默认断言（PRD 风险表"测试混用生产配置"正解） | 2026-09-01 |
| 板块聚合（十八期） | `fetch_sector_heat` 内部单点聚合：SECTOR_MAPPING（10 大类 + 新浪实际别名）× `_parse_turnover` 还原成交额 + `aggregate_sectors` 纯函数加权；日报/快照/开盘分析/context/web 五个消费点零改动（web 只读 context 无聚合源，必须取数层聚合）；聚合公式 = Σ(子板块 change×成交额[元]) / Σ成交额[元]，Σ=0 走简单平均；未匹配概念板块归「其他」参与排序；聚合行契约 {name, change, turnover, top_stock} 与概念行同构；top_stock 取类别内成交额最大子板块 | 聚合在取数层一次完成，消费点自动全变大类，零 mock 签名破坏、零重复实现；精确匹配兜底漏配；大盘名替代概念名（超出 PRD 文件表字面，已确认） | 2026-08-31 |
| 美股去重 + 浅色主题（二十五期） | 美股去重：判定符号集取 GSPC+IXIC（排除 MOVE 浮点抖动 70.965→70.9655），`daily_report.py` 新增纯函数 `_is_us_duplicate_day(history, record)` 在 `append_history` 前加门，非交易日（美股值全同）整条跳过写历史（D1/D2）；混合日（美股未动但 A 股/BTC/GLD 变动，如 09-01）整条跳过、PRD 字面取舍、当日日报/context 仍完整；浅色主题：`web/static/style.css` 加 `:root.light` 变量覆盖 + `.card` 白底，前端在 `index.html` 加预应用脚本（防 FOUC）+ topbar 右上 `🌙/☀️` 切换按钮 + `setTheme(light)`（切 `html.light` 类 + 写 `localStorage["mp-theme"]` + 更新图标） | 美股重复特指 GSPC/IXIC（实测 08-30 与 08-29 全同、09-01 与 08-31 全同）；MOVE 浮点抖动会误判故排除；方案不引入交易日历依赖、不改 src/、零后端变更、web/app.py 不动；浅色主题纯前端、涨跌色（--green/--red/--blue/--orange）不变 | 2026-09-01 |
| cron 自动提交推送（二十六期） | 新建 `src/git_ops.py`，三入口 main() 末尾调用 `auto_commit_push(date, report_type)`；默认开启（`AUTO_PUSH`≠"0"），`AUTO_PUSH=0` 关闭；无改动跳过（幂等，NF3）；`git add -A` 全量语义；commit message 全 ASCII `auto: {date} {type}`（daily→`daily report`、snapshot→`{market} {time} snapshot`、opening→`{market} opening analysis`，F5）；push 经 Clash 代理 `http://127.0.0.1:7890` 且仅注入子进程 env 副本（F3）；每步 subprocess timeout（15/30/120s）；失败不抛异常、print `[auto-push] Failed`、退出码恒 0（F6 重试由 `scripts/push_retry.sh` + Hermes cron 承担）；pytest 经 conftest `AUTO_PUSH=0` 隔离，无真实推送；零新依赖（纯 stdlib，NF1） | 防 Railway 与最新数据脱节；避免三入口重复实现、单测可覆盖；缺省开保证 cron 自动同步落地；`AUTO_PUSH=0` 供本地开发隔离 | 2026-09-02 |
| 日间合并写 history + 读时剔除（二十七期） | 新增 `analyzer.merge_history(date, values)`：仅把本入口市场子集（a-share={SH,SZ,CYB}、us={GSPC,IXIC}；alt/GLD/BTC 与 VIX 不写 history）并入当日行，写前按 `_HISTORY_KEYS` 投影、不整行覆盖、保留同日其它入口数据；取数全失败（values 空/全 None）→ 空操作、绝不制造空行。三入口读 history 均先剔除自身 date 行再用于趋势图/相关性/连涨/告警基准，避免同日多入口重复叠加或引用盘中残留 | 收盘日报历史末点现含盘中实时价而非仅收盘快照，跨市场联动可见；零整行覆盖、零空行、零并发冲突（R1-R6） | 2026-09-03 |
| Web 看板自选股（二十八期） | `web/app.py` 新增 `@app.get("/api/watchlist")` 第 4 个 JSON API：实时取数 `config.json` 的 `watchlist.stocks`（A 股经 AkShare、美股/ETF 经 Yahoo），返回 `{stocks:[{symbol,label,value,change_pct}], trend:{dates,series:[{key,label,values,change_7d,raw}]}}`（trend 与 /api/history 同构、归一化基准 100）；`web/templates/index.html` 板块热度后新增自选股 card（初始隐藏、实时取数、单图多标的 spanGaps）；纯函数 `_build_watchlist_payload`/`_load_watchlist` 容错降级空结构（HTTP 200 不 500）；前端失败行显示「数据暂缺」；响应新增 hidden 键（无配置→true 整卡隐藏；有配置→false 必显卡，取数失败/超时/网络错前端显示「数据暂缺」占位而非静默隐藏）。 | 复用现有 web 层与日报 `_build_watchlist_view` 同涨跌幅公式；零写盘、零侵入日报/快照主流程；配置为空则前端整卡隐藏（与 watchlist「可选体验」定位一致） | 2026-09-04 |
| 日报定稿保护（本任务） | `analyzer.append_history(record, merge_existing=False)` 新增 `merge_existing` 参数：日报定稿（盘中美股未收盘 fetch 为 None）时若当日行已有快照写入的盘中值（GSPC/IXIC 非 None），保留之，避免整行覆盖为 None 导致 web 回退旧收盘；补全仅限 `_HISTORY_KEYS` 且排除 date，默认 False 其余调用零影响；`daily_report.py` 调用点传 `merge_existing=True` | 美东盘中跑日报会抹空快照已写入的美股盘中值（整行覆盖），web /api/latest 误显 09-03 旧收盘；前置兜底，调度归位后自然不再触发 | 2026-09-04 |
| Web 看板来源标注（本任务） | `/api/latest` 的 indices 项新增 `source_date` 字段：末行某符号前向回填时记录来源行日期；`web/templates/index.html` 的 `renderOverview` 对回填行 value 后追加「（MM-DD收盘）」小字、涨跌幅格显示「未收盘」，避免与概览日期错配的观感 | 回填值仍显真实收盘值但无来源说明，易误读为当日数据；向后兼容（旧前端忽略未知键） | 2026-09-04 |
| 美股午盘快照日期门 + Yahoo 403 双主机轮换（本任务，2026-09-05） |
| 前端 UI/UX 重构（2026-09-05） | 看板重构为 Professional Financial Dashboard×现代 FinTech×Apple 克制：flex shell（侧栏 220-240px + 主区 flex:1）+ 纯前端 Sidebar（6 内联 SVG 图标零依赖 / 锚点滚动 / 移动端抽屉）；双主题 token（Light #F7F8FA 优先完整 / Dark #0B0F14 分层），图表色 COLORS 随主题取数组并切主题重渲染；概览表回 5 列（指数/最新价/涨跌/涨跌幅/状态，涨跌前端逆算 value/(1+chg/100)、趋势文本入状态列）；KPI 卡片化；移动端 KPI 两列 / 图表单列 / 表格横滚 / 主图高≥280 防扁条。仅改 web/templates/index.html 与 web/static/style.css，不动 web/app.py / src/* / data/*，不引新依赖。 | 看板视觉与响应式现代化，贴合 PRD §一~§二十验收清单；纯前端重构不触业务后端。 | Hermes cron「美股午盘快照」prompt 改按美东交易日定位并校验（`TZ=America/New_York date` 取美东日期、删除"日期==北京今天"比较）；`src/fetcher.py` 新增 `YAHOO_HOSTS=("query1","query2")` + `_yahoo_chart_get` 逐主机轮换（403/429/5xx/连接错误/超时切下一主机，404 等确定失败立即 `raise_for_status` 不浪费轮换），4 个 Yahoo chart 调用点（fetch_vix_vxn / fetch_vix_realtime / fetch_us_sector_heat / _fetch_yahoo_watch）改走 helper，`_SESSION` UA 换浏览器串 + `Accept: application/json`；`src/reporter.py` 单市场快照 None 单元格真值化（us/alt→数据暂缺、a-share→休市） | 根因：快照脚本日期本按市场时区正确（analyzer.get_market_date 七期起），"跳过推送"真因在 Hermes prompt 用北京日期比对美东文件名（北京 00:00=美东前一日，必然不匹配）；Yahoo 单主机 403 重试逃不出主机级封锁，双主机轮换把"单主机瞬封"从必然失败变可自愈 | 2026-09-05 |
| Web 看板 Bento 重构（2026-09-11） | 前端三文件重写：`index.html`（141→262 行，`.dash` + 4 视觉行栅格、9 类模块、3 个 `data-placeholder="1"` 占位）/ `style.css`（475→约 520 行，卡片 token 化 `--radius-card`+`--card-shadow`+`--card-glow` 双主题、断点重写、删失效规则）/ `app.js`（823→约 700 行，**四图合一 + 4 类别 tab**、KPI sparkline、CSS 迷你条、修 C6）；`web/app.py` 新增 `_sector_payload(ctx, key)` 纯函数（`_load_sector_heat` 改薄封装、签名不变）、`/api/latest` 同源暴露 **`us_sector_heat`**（C7：context 早有键但端点从未暴露）、新增 **`/api/macro`**、`/api/history` 上限 `le=90→365`；`tests/test_web.py` 48→59 条（`days=91` 由 422→200、新增 `days=366→422`、`us_sector_heat` 3 条、macro 7 条）；新增 `tasks/2026-09-11-frontend-bento-redesign/verify_ui.py`（Playwright 验收脚本，自动挑空闲端口起服、三视口断言、产物落 `$env:TEMP`）；零新依赖、零二进制资源 | 1920×1080 `scrollHeight` **2632→1235**（2.44 屏→1.14 屏）、1280 `2655→1819`、三视口零横向溢出、console error 0、深浅主题与 4 tab 全绿；`pytest tests/` **459 passed** | **C2 根治**：删除 `.chart-box canvas{…!important}` 改为 `#chart-main-wrap{height:clamp(300px,40vh,460px)}` + `maintainAspectRatio:false` → canvas 位图 == 显示尺寸（原 1280 档纵向拉伸 1.405×）；**C6 根治**：`isWeekendDate` 用 `Date.UTC`+`getUTCDay` 按**数据日**判周末（原按浏览器当天）；栅格列一律 `minmax(0,1fr)` + 卡片 `min-width:0` 防 min-content 撑破；删除原「10 指数概览表 + 排序 + 指标 chips + 分组可见集」（被 6 小卡 + 4 tab 替代，10 标的仍全部可达）；tab 切换**原地切类**不重建 DOM；未做 1Y 回填（`seed_history.py` 存 R5 前置缺陷） | 2026-09-11 |
| 1Y 历史回填（Step 10，2026-09-11） | ① `config.json` 的 `history.retention_days` `90→365`（`config.json` 被 `.gitignore` 排除，只影响本机 = 唯一的历史写入方；`.env` 无 `HISTORY_RETENTION_DAYS` 覆盖）；② 新增 `scripts/backfill_history.py`（**方案 B**：不改早期 `seed_history.py`）：走 `_yahoo_chart_get` 双主机轮换 + 按符号所属市场时区归档日期（A 股→`SHANGHAI_TZ`、其余→`EASTERN_TZ`）+ **只新增 date 不存在的行** + 丢弃「仅 BTC 有值」的非交易日（BTC 7×24 日线含周末）+ `merge_history` 按 date 合并 + 收尾按 date 升序重排与断言 + **不调用 `save_last_values`**；③ `verify_ui.py` 新增「1Y 档渲染 ≥200 交易日」断言 | 1Y 视图从 90 点 → **259 个交易日**（2025-09-11 ~ 2026-09-11）；`/api/history?days=365` 有效；回测有效交易日 90 → **261**（触发事件 304）；既有 90 行逐键比对**零改动**、`last_values.json` SHA256 **未变**、新增周末行 **0**、date 严格升序无重复；`pytest` 459 passed；`verify_ui.py` ALL PASSED | **plan §7 Step 10 / R5 有三处漏识别**（见 `tasks/2026-09-11-frontend-bento-redesign/plan.md` 附录 A）：S1 时区不能统一美东（A 股会整体早一天）、S3 `append_history` 整行覆盖会抹既有值、S4 `seed_history.py` 小写键覆盖 `last_values` 会让次日涨跌幅与告警全失效；执行中另发现 **S6：`merge_history` 只 append 不排序**（回填历史日期必须收尾重排，否则 `/api/latest` 把最旧日期当"最新日"）。**CYB 缺口已闭环**：`399006.SZ` 的 Yahoo 1y 只返回 1 天 → A 股标的 Yahoo 覆盖 < 30 天时改走 AkShare `stock_zh_index_daily`（daemon 线程 15s 限时、窗口按 Yahoo 最早日期裁剪且排除 BTC 自然日、`000001.SS→sh000001` ticker 换算），并把补到的 A 股键**只写进既有行中该键为 `None`** 的位置（157 键，非 cyb 键改动 0 / 非法覆盖 0），`cyb 86/259 → 243/259` 与 `sh`/`sz` 完全对齐 | 2026-09-11 |
| 黄金报价位 GLD→COMEX（三十四期，2026-09-13） | 两处黄金展示位换 `GC=F`：① 概览卡「黄金 ETF」（GLD×10 画图缩放值，语义错位）→ `GC=F` 实时（macro 链路，`_MACRO_DEFAULT` +4 项）；② 另类资产趋势图黄金线：history `gld` 序列 → `/api/macro` 的 `gc=f` 归一化序列（图例「黄金 COMEX」，色沿用 `--c-gld`），日期轴取 history ∪ macro 并集、gc 逐日投影 + filled 前向填充；macro fetch 成功后补 `renderMainChart()`（覆盖 macro 晚到且用户停在 alt tab 的时序）。GLD 保留在 history/SQLite 作 EOD 历史（`/api/history` 的 gld 序列保留输出，UI 不再消费），日报另类资产板块不动 | 报价语义（实时现货价）与 EOD ETF 净值分离；方案 A 前端叠加零后端/零数据管线改动，方案 B（SYMBOLS 换 GC=F 写 history）爆炸半径大且归一化后曲线形状无差异，否决。配置覆盖陷阱：env `MACRO_STOCKS` > config.json > 默认，任一覆盖存在时改代码默认值不生效（本期 config/env 均未设宏观数据，默认生效；Railway env 需自查） | 2026-09-13 |
| 看板资讯契约先行（三十三期，2026-09-12） | 新端点 `GET /api/news`（web/app.py `NEWS_FILE`=`data/news.json` + `_load_news`：容错空态 HTTP 200 恒定、条目过滤 title 非空 + url http(s) 前缀防 javascript: 注入、缺省字段补 ""、cap 8）；前端 `fetchNews`（仅初始加载一次）+ `renderNews`（`escapeHtml` 全量、`target=_blank rel="noopener noreferrer"`、摘要 line-clamp 2、`#news-date` 数据日期）。**资讯契约（Hermes 侧落盘规格，用户改 cron prompt 的唯一依据）**：`data/news.json` = `{"date": "YYYY-MM-DD", "items": [{"title", "url", "source", "published", "summary(≤80字)"}], 3~8 条按重要性排序，title/url 必填，tmp+rename 原子写}`。**所有权边界**：`context/*.json` 只归 Python（generate_context）、`data/news.json` 只归 Hermes（Python 无搜索能力，决策 G）——双写者互不越界。未接入前卡片常驻空态「暂无资讯」即验收态，Hermes 接入后自动点亮；Railway 侧资讯延迟数小时属预期（随下次 Python 入口 auto-commit 入库） | 结构化资讯可渲染标题/链接/摘要；「解析日报解读章节」方案已证伪（章节不存在且 prose 不可拆）；写 context 会被快照覆盖（双写者冲突）；Python 直连 tavily 违反决策 G | 2026-09-12 |
| 历史存储 SQLite 化（三十一期，2026-09-12） | `data/history.json` → `data/marketpulse.db` 长表 (date, symbol, value, change)，PK(date,symbol)+WAL；`analyzer.load_history/append_history/merge_history` 三函数签名不变只换内部（16 消费方自动跟随，reporter 零改动 D4）；永久保留（HISTORY_MAX 裁剪废止 D7）；`/api/history` 加 start_date/end_date（显式范围优先，days 上限 365→3650 D9）；备份 = `data/backup/history_YYYY-MM.json` 按月归档当月覆盖/历史月冻结（D1），daily 生成 context 后调用、随 auto-commit 入库；web startup `restore_if_empty` 三级恢复（备份合并→旧 JSON 兼容读→空库）（D2 必做，Railway 临时文件系统下是主数据源）；change 列保留一律 NULL（D3），change_pct 读侧派生；迁移脚本 `scripts/migrate_to_sqlite.py` 逐值校验幂等；测试 patch 点 14 文件迁 `storage.DB_PATH`/conftest `tmp_db`（R1 主体工程）。**架构权衡（D7）**：Railway 持久性从"git 随行"变为"备份恢复链"，接受 push 失败期间部署拿到旧数据（push_retry 兜底）；backtest 输入冻结为已知缺口（D8） | 结构化查询 + 日期范围 + 永久保留（用户 PRD）；爆炸半径压缩到 analyzer+web 两处 | 2026-09-12 |
| 自选股看板文件化（三十期，2026-09-12） | 新增 `analyzer.WATCHLIST_FILE`（`data/watchlist.json`）+ `save/load_watchlist_snapshot`（存原始数据：stocks/values/series，saved_at 仅展示；stocks 空 / values 全 None → 空操作防垃圾覆盖；原子写）。写入点：`daily_report.py`（fetch_watchlist 成功路径内，try/except）+ `snapshot_report.py`（context 块后、auto_commit_push 前，新鲜度随 4 cron 对齐）。`web/app.py`：`_load_watchlist` 重构为「快照优先 + symbol 配置比对 + 实时回退」，配置读取抽 `_watchlist_config()`（env > config.json 原语义），命中响应加 `as_of`；`_build_watchlist_payload` 等纯函数零改动；`load_watchlist_snapshot` 模块级导入到 web.app（monkeypatch 打使用方）。前端：副标题时点标注 `#watchlist-asof`（MM-DD HH:MM）、「现价」→「最新价」、空态去「（实时取数失败）」。测试：test_analyzer 9 条（roundtrip/覆盖/空守卫/容错）+ test_web 3 条（文件命中零联网 / mismatch 回退 / 无快照回退），`_reset_watch_cache` fixture 默认把快照读取隔离为 None（本机真实快照不得劫持未打补丁用例） | 打开看板自选卡与其他卡片同为毫秒级（原实时路径 TTL 90s 过期后 2~10s 联网）；语义变更（实时→最近报告时点）用户已确认，`as_of` 防误读；Railway 上 data/ 随 auto-push 入库故文件化成立；休市日 gate 跳过刷新属预期（plan 风险 9） | 2026-09-12 |
| 宏观数据独立页 /macro（2026-09-14） | 新增 `GET /macro`（**第二个页面**）+ shell 抽取：顶栏/侧栏抽成 `_topbar.html` / `_sidebar.html` 两个 include（`base_prefix` / `active_page` 参数化；**单文件 `_shell.html` 不可行** —— 侧栏必须留在 `.shell` 网格内、顶栏在其外，一个 include 只能出现跨文件未闭合标签）；首页「宏观数据」由 `is-disabled` 占位改为 `<a href="/macro">`。数据层：`_fetch_yahoo_watch(symbol, range_="2y")` / `fetch_watchlist(stocks, range_="2y")` 参数化（**默认值不变**，自选股零影响），`/api/macro` 传 `range_="5y"` + `tail=1260`（实测 `trend.dates=1258`、响应 205 KB），`_MACRO_TTL` 90s→**300s**。新增 3 个纯函数：`_compute_macro_regime`（四维度 -2~+2 → ÷8 归一化 → ±0.25 三态 + `score100`）、`compute_macro_correlation`（**1 年滚动窗口**、固定 6 对、复用 `analyzer._returns/_pearson`）、`_macro_history_regime`（逐日回放、复用 `_compute_risk_appetite`）；三者与 `stocks`/`trend` 合并进同一响应。前端新增 `macro.js` + `style.css` 的 `.mac-*` 段（research terminal：大留白 / 细边框 / 无彩色卡片堆叠 / 主图占视觉中心）。`_asset_version()` 改读 `_ASSET_FILES`（新增前端文件必须加进去） | Macro Score 采用 Equicurious《Using Risk-On/Risk-Off Dashboards》四指标法，**阈值是工程取值**（信用利差维度用 波动率/美元/利率/商品 替代，因 FRED 本机不通）；宏观关系用业界惯用的 1 年滚动窗口；**四象限/通胀轴/增长轴只在服务端算**（`src/econ_fetcher.py`），前端只消费 —— 防前后端漂移。⚠️ **`^TNX` 口径实测澄清**：Yahoo 该接口返回的就是百分数（实测 4.985 = 4.985%；5 年前 1.277 对应 2021-09 真实 ~1.3%）→ **不换算**；显示层 `toYieldDisplay` 只做量级自适应（绝对值 >20 才 ÷10）+ `console.warn` + 断言锁口径（原 plan 的「收益率×10 必须 ÷10」是常识误植，照做会显示 0.4985%） | 2026-09-14 |
| 经济数据源 BLS（2026-09-14） | 新增 `src/econ_fetcher.py`（约 230 行）+ `GET /api/econ`（web/app.py，模式对齐 `api_macro`）：**一次 POST** `api.bls.gov/publicAPI/v2/timeseries/data/` 拿 4 个序列 —— CPI-U `CUUR0000SA0` / PPI 终需求 `WPSFD4` / 失业率 `LNS14000000` / 非农合计 `CES0000000001`（免 Key、零新依赖、零写盘）；派生 `latest / yoy / prev_yoy / direction / history` + 四象限（`inflation_axis`×`growth_axis` → `quadrant` + `quadrant_label`）。**三条硬约束**：① `_ECON_TTL = 6*3600`（BLS 无 Key 限 **25 次/日/IP**，90s TTL → 960 次/日必超限；经济数据月度，6h 无损新鲜度），**只缓存成功结果**（`as_of` 空不写缓存，否则一次抖动锁死 6 小时）；② CPI-U/PPI 返回**指数**，同比自算，且**按月份键找去年同月**（`"2026-08"`→`"2025-08"`），**不用"取第 13 个"**；③ `as_of` 取**数据月份**（`"2026-08"`）而非抓取时间（经济数据有发布滞后）。增长轴用**就业替代** GDP/PMI，`basis` 字段如实标注「非 PMI」 | 实测 2.1s、4 序列全部 2026-M08；**否决** FRED（服务正常但本机不通：直连 20.9s / Clash 30s 均超时）与 AkShare `macro_usa_*`（除 `macro_usa_cpi_yoy` 外全部停更在 2025-09 且 9.5~28s，超所有超时预算；`cpi_yoy` 留作通胀轴降级备用）。**执行中实测到 BLS 真实缺月：CPI-U 与失业率都缺 `2025-10`（43 vs 完整 44 条）** → 按位置取第 13 个会静默把错月份当基准（同比算错且不可见），故改按键查找 | 2026-09-14 |


| 自动提交范围收窄为路径白名单（三十四期，2026-09-14） | 起因：外部 Hermes 5 分钟 cron（`MarketPulse 自动推送GitHub`，id `6f6e40a6f8b4`，`source=builtin`）与三入口共用的 `src/git_ops.py` 都做 `git add -A` → 把工作区里**正在写的**源码/测试/文档半成品一并入库（实测扫走过 `web/static/style.css`、`tests/conftest.py`、`docs/pitfalls.md`、`tasks/*/journal.md`）。改动：`src/git_ops.py` 新增 `_DATA_PATHS = ("data", "context", "alerts")` 白名单；`_commit` 由 `git add -A` 改为 `git add <白名单>`；`_has_changes` 由全量 `git status --porcelain` 改为 `git status --porcelain -- <白名单>`（**两者必须同范围**，否则"仅源码 WIP"时误报 `Failed` 且返回值语义错标）。`auto_commit_push` 签名与三调用点（`daily_report.py:263` / `snapshot_report.py:98` / `opening_analyzer.py:122`，均位置参数 2 个）**零改动**；AUTO_PUSH 门控 / 代理注入 / 15-30-120s timeout / 失败返回 False 全部不变。`reports/` **有意不在白名单**（`.gitignore:40` 排除它，而 ignored 路径作显式 pathspec 会直接 `fatal: pathspec … did not match any files` → 三入口数据提交全挂）。测试 `test_phase26.py` 11→**16** 条（既有 11 条零改动 + 新增 5 条钉死白名单实参/status 同范围/源码 WIP 不触发/reports 不入列/忽略文件不触发）。**G1 验证**：临时仓库真实 git 冒烟（源码 WIP 未被提交、且仍留在工作区）+ 本仓库只读验收（现场存在 `M snapshot_report.py` / `M src/reporter.py` 时白名单口径为空 → `_has_changes=False`） | 原诉求是"改 cron 时间"，实测**时间不是根因**（数据 24h 流动，无安静时段），根因是**提交范围**：收窄 add 后外部高频抓取可保持 `*/5` 不变，数据新鲜度（G2）零劣化。⚠️ `git add -A` 的全量语义自本期起仅保留在**仓库外** Hermes 配置（步骤 5 待用户处置 → 残余漂移风险，记入 `system-overview.md` §9 G7） | 2026-09-14 |

| 中国宏观独立页 `/macro/cn`（2026-09-14） | 新增 `src/cn_econ_fetcher.py`（AkShare 13 序列：CPI/PPI/PMI/GDP/M2/社融/新增信贷/失业率/社零/房价(北京·上海)/LPR/SHIBOR/10Y国债；**与 `src/econ_fetcher.py`(BLS) 平行不共享取数**，只把四象限常量提升为公开 `QUADRANTS` 复用）；新增 `web/templates/macro_cn.html` + `web/static/macro_cn.js`（复用 `_topbar`/`_sidebar` 与 `.mac-*`，仅补 ~16 行 `.cn-*`）；`web/app.py` 新增 `GET /api/econ/cn`（`?group=` 分组）、`GET /api/cn/quotes`（CNY=X + 中债 10Y + 信用利差）、`GET /macro/cn`；侧栏新增同级项「中国宏观」（navCount 10→11，`verify_ui` F-5 由单值 `macroHref` **补强**为数组 `macroHrefs`）；新增 `tests/test_cn_econ.py`（14 条）、`scripts/probe_cn_macro.py`（13 在册 + 8 否决接口的可重跑探针）。**增长轴口径与美股版不同**：PMI 与 **50 荣枯线比较（水平）**，不用同比方向（49.8 的水平=收缩，而它同比 +0.81%=扩张，两者结论相反）；GDP 同比方向作交叉校验并如实上报 `conflict`。**缓存语义与 BLS 版有意不同**：13 个接口相互独立 → 只有「全部失败（as_of 为空）」才不缓存，部分失败照常缓存并走 `failed` 逐模块降级；失败的 key **不写 ts**（避免一次抖动锁死 6h） | 实测推翻两个先验假设：① `macro_china_new_house_price` **只有北京·上海 2 城**（不是 70 城），且主列是「上年同月=100」指数 → 同比% = 值−100；② 增长轴不能照抄美股版的同比方向。**取数性能**：串行 22.2s / workers=6 → 13.6s / workers=13 → **10.3s**（墙钟由最慢的 `bond_china_yield` 8s 决定）→ 超 8s 判据，启用 R1 分组端点，前端**分两波**加载（⚠️ 6 组同时发无收益，13 个上游请求照样在飞）。另有实测：**排序口径不统一**（cpi/ppi/pmi/gdp/m2/credit/retail 倒序，其余正序）→ 一律按周期键排序后取尾 | 2026-09-14 |

| 利率类「变化」改口径为 Δ6M(bp)（2026-09-15，用户反馈落地） | 起因：用户看「利率与流动性」发现 `+21.20% ↓`（加号配下降箭头）像 bug —— 取证后确认是**一格两个口径**（颜色/正负号 = 同比本身；箭头 = 同比 vs 上期同比）且无标签；顺带发现**10Y 国债的同比恒为 `—`**（中债接口只有 6 个月窗口 → 取不到去年同期基数 → 一格死数据）、LPR 同比恒 `0.00%`（政策利率一年不动，看起来像坏数据）。改动：`src/cn_econ_fetcher.py` 新增 `_chg_6m_bp` + 三条利率序列标 `chg_bp: True`（其余序列该键恒 `None`，**不改数据契约的其他部分**）；`lpr` 的 `freq` 由 `day` 改 `month`（实测其发布频率 2014-2018 每日、2020 起每月，120 点 = 6.6 年）；前端 `chgCell(s)` 统一出「文本 / 颜色 / title」，利率显示 `±X.Xbp`（单位自带，**不配箭头**）、其余仍「同比 % + 箭头」；列说明写明两种口径。**口径消歧与新增口径是两件事**：前者修「箭头 vs 正负号」（title + 列说明 + 断言），后者修「利率不该用同比%」 | 改造后真实值：LPR **0.0bp**、SHIBOR **+11.44bp**、10Y 国债 **−8.96bp**（此前分别是 `0.00%` / `+3.65%` / `—`）。⚠️ **基准点必须按日期/月份定位**：LPR 120 点跨 6.6 年，按位置取会算出"7 年累计 −131bp"。代价（有意接受）：同一列出现两种单位 → 靠"数值自带单位"自描述。验证：`pytest` 663 passed（新增 4 条口径用例）、`verify_ui` CN-4f/4g/4h 全绿 | 2026-09-15 |


| 悬停横线吸附到数据线（2026-09-15） | `web/static/chart-crosshair.js`（三页**共享**的内联插件）：`afterEvent` 不再取鼠标 y，改为 `snapToNearest(chart, e.x, e.y)` —— **先按 x 定唯一数据列**（tie 取较小索引）**再取该列 y 最近的可见数据集**；缺口（整列 NaN）依次试 ±1..±3 列，仍无则回退鼠标 y；`$crossY` = 吸附点 y，新增可测挂钩 `$crossSource = {dsIndex, dataIdx}`；节流对象由鼠标 y 改为**吸附后的 y**（阈值 0.5px）；`afterDatasetsDraw` 线色取吸附系列的 `borderColor`(α.65)、无目标回退中性轴色，并在吸附点画 3px 圆点。**推翻旧设计「一条中性色线 + 一个轴读数、与系列无关」**：吸附后线必然属于某个系列，中性色不再成立（原设计在"线不属于任何系列"时才自洽）。轴端读数公式**逐字不变**（仍 = `formatter(getValueForPixel($crossY))`），但语义由「鼠标高度处的轴值」变为「该数据点的值」→ 顺带修掉单数据集图**读数与 x 无关**的缺陷（实测旧行为横向移动恒读 3950.22）。坐标一律 CSS 像素，**不乘 devicePixelRatio**。验收：`verify_ui.py` 的 CS-2 拆为 CS-2a/2b、XC-5 拆为 XC-5a/5b（旧判据「同 x 不同 Y 指纹必不同」= 跟手，吸附后语义反转），新增 CS-8/8b/9/10、CS-D1~4（DPR=2 取证）、CNC-0~6（`#cn-chart` 此前零 crosshair 覆盖） | 用户诉求「横线吸附在线上，随鼠标在线上移动」；实测基线：横线可离最近数据线 **268.9px**、`/macro/cn` 读数恒为同一值。⚠️ 独立探针必须与 Chart.js **同口径取整**（`getRelativePosition` 对事件坐标做 `Math.round`），否则并列处探针取 idx=32、插件取 33 | 2026-09-15 |

| 侧栏市场状态改「两行两市场 + 逐市场时段判定」（2026-09-16） | `web/templates/_sidebar.html` 的 `.market-status` 由 1 行改 2 行（`#market-dot-cn`+`#market-status-cn` / `#market-dot-us`+`#market-status-us`，各带 `data-market`）；`app.js` / `macro.js` / `macro_cn.js`（三份**逐字副本**）的 `updateMarketStatus` 重写为 `MARKET_SESSIONS` 配置 + `marketLocalParts/marketSessionOf/marketStateOf/marketBeijingHM` 四个函数（`session ∈ weekend\|midday\|open\|pre\|post`，边界含左不含右，周末按**该市场 local weekday**）；新增验收同源钩子 `window.__marketSession(iso)`（传 ISO 即注入假 `now`）。**点色语义变更**：原 `open` = 「今天是工作日」（全局单点）→ 新 `open` = 「**该市场此刻在交易**」（逐市场），"任一市场在交易" ⇔ "至少一颗绿"，**不加第三个总状态点**。`style.css` / `web/app.py` / `src/**` / `tests/**` **零改动**（实测 `.ms-row` 已是 flex row、类名复用；footer 45→63、`#sidebar` 不出纵向滚动条、页面总高 1216 不变） | 用户报「20:40 显示『市场已开盘』，但 A 股已收盘、美股未开盘，且文案无主语」→ 实测是缺陷不是文案偏好：旧代码取出的 `hh/mm` **从不参与判定**。**明确不做**：盘前/盘后四态（会让人以为散户可交易）、节假日判定（需引日历依赖，违反"不引入新依赖"，"休市"只用于周末）、硬编码北京 21:30（夏令时静默错 1h）。验收 `MS-*` 47 条含 9 例注入时刻覆盖表（2 例 12 月 DST 护栏 + 1 例跨时区周末）+ 三页逐字一致 + 三档几何护栏 | 2026-09-16 |

| 宏观页四态 + 因子 chip + Score 语义色（2026-09-16，只做新增） | `macro.html` 预置**静态骨架屏**节点（`data-skel="<block>"`，复用既有 `.skeleton`，不新增骨架体系）+ 失败条 `#mac-fail-bar`（含 `#mac-retry`）；`macro.js` 引入四态状态机（`loading / ok / empty / failed`，`SKEL_OF_SOURCE` 定义"取数源 → 结算时要撤掉的骨架块"，`renderAll()` 末尾按 `failMacro/failEcon` 统一补挂 `is-failed`）+ `assetChipsHtml()`（因子「影响资产」→ 方向 Badge + 资产 chip，复用既有 `dollar/bond/gold/oil.svg`，颜色取主图同款 `--c-*`）+ `#regime-score` 按 `LEVEL_CLASS` 挂 `up/down/flat`；`style.css` 新增规则**一律 `#mac-*` / `#macro-*` id 限定**（不动任何 `.mac-*` 公共声明，`/macro/cn` 零影响）。**可注入超时钩子** `window.__macroTimeoutMs`（生产 15000ms）让验收在 2s 内真跑一遍超时分支 | 起因：首屏空白/字面 `—`、取数失败只有一行「图表加载失败」。**三条硬约束**：① shimmer **只用于 loading**，"取不到数"必须落到既有 `.mac-empty`（类名与「数据暂缺」文案是 `verify_ui` 的可测契约）—— 用动画掩盖故障比空态更糟；② **15s 超时必须切到 failed**，否则上游 403 常态下页面永远停在骨架屏（R1）；③ `#regime-score` 颜色**由服务端 `level` 同源推导**，禁"`score100 > 50` 就染绿"（`score100` 是 0~100、中性 50，见 `web/app.py`；±2 是因子 `impact`）。**明确不做**（出范围）：Hero 卡片化、全局提对比、卡片套卡片、JetBrains Mono/webfont、SOON 徽章（已实现）、改默认主题、后端改动。验收 `NA-*` **32 条**（含 4 组 Score 用例其中 2 组为"level 与 score100 矛盾"的反向护栏、mock 四态、块级 `is-failed`、`/macro/cn` 零影响）；实测 `#mac-factors` 留白 12px 不变、`docH` +10px、`chartWrapH` 497/340/390 不变 | 2026-09-16 |

## 约束

- 不修改的模块/文件: `.env`，`reports/`，`data/`（均为运行时生成/用户配置）。
- 必须保持的兼容性: `python daily_report.py` / `python snapshot_report.py [--market a-share|us] [--time open|midday|close|noon]` 全程可手动运行（裸跑 = 美股午盘）。
- 依赖边界：运行依赖 `requests` / `matplotlib` / `pytest` / `fastapi` / `uvicorn` / `jinja2` / `httpx`（十一期 Web 看板引入后四项，PRD 技术栈明确指定 FastAPI + uvicorn + jinja2，httpx 为 TestClient 必需；Chart.js 经 CDN 不落盘，不计入 Python 依赖）。
- 五期起：阈值全部外置 `config.json`，`src/config.py`（约 122 行）+ `analyzer.py`/`reporter.py` 接线（约 +5 行）；`tests/test_config.py`（约 180 行）。优先级链 env > config.json > 内置默认，零新增依赖；`config.json` 入 gitignore（用户定制不入库）。

## 目录级规则

- `reports/`、`data/`、`alerts/` 与 `context/` 为生成物，默认 `.gitignore` 排除或运行时自动创建。
- 每个函数只做一件事，带 docstring。
