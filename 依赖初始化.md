# 项目初始化命令（可直接复制执行）


## 1. 依赖清单 (`requirements.txt`)

```txt
yfinance>=0.2.40
requests>=2.31.0
python-dotenv>=1.0.0
pytest>=8.0.0

2. 项目初始化命令
# 创建项目目录
mkdir marketpulse && cd marketpulse

# 创建虚拟环境
python3 -m venv venv
source venv/bin/activate  # Windows: venv\Scripts\activate

# 安装依赖
pip install -r requirements.txt

# 创建目录结构
mkdir -p reports data

# 创建环境变量模板
cat > .env.example << 'EOF'
FRED_API_KEY=your_fred_api_key_here
EOF

# 复制为正式环境变量（填入你的 Key）
cp .env.example .env

3. 目录结构
marketpulse/
├── daily_report.py          # 主脚本
├── requirements.txt         # 依赖清单
├── .env.example             # 环境变量模板
├── .env                     # 环境变量（gitignore）
├── reports/                 # 报告输出目录
│   └── YYYY-MM-DD.md
├── data/                    # 数据存储目录
│   └── last_values.json
├── tests/                   # 测试目录（预留）
└── README.md                # 项目说明（可选）

4. 开发/测试/构建命令
用途	命令	说明
激活环境	source venv/bin/activate	Windows: venv\Scripts\activate
运行主脚本	python daily_report.py	执行完整流程
运行测试	pytest tests/ -v	目前预留，后续加
检查依赖	pip freeze	查看已安装的包
导出依赖	pip freeze > requirements.txt	更新依赖清单
退出环境	deactivate	退出虚拟环境